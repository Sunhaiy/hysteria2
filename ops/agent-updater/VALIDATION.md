# 验证与上线记录 — 2026-09-15

主站功能已部署，六台在用节点均已安装独立更新器并完成真实版本下发。

- API：15 项新增测试及 1 项密钥迁移回归通过。PostgreSQL 测试使用本机数据库的
  隔离 schema，验证真实事务锁、并发领取、首台门禁、幂等重放、离线占位、
  回滚占位、取消、权限、CSRF、签名、下载归属与状态校验。新增迁移在隔离
  schema 执行成功，权限迁移重复执行不会重复授权，普通用户不会被授权。
- 更新器：Linux amd64 环境下 11 个顶层测试通过，包含 4 个崩溃恢复子场景。
  使用真实磁盘日志、原子符号链接替换和 HTTP 测试服务器；进程重启及健康
  检查依赖为测试替身，不声称完成真实生产 systemd 更新。
- 安装器：4 项测试通过，覆盖首次安装、启动失败恢复覆盖配置、错误架构、
  非 root Agent 拒绝。测试文件系统和 systemctl 均隔离。
- 构建：API/Web 生产构建及类型检查通过；相关源码 Lint 通过。
  独立更新器 Linux amd64 和 arm64 均交叉编译成功；arm64 未做原生运行测试。
- 页面：使用本地只读数据接口检查生产构建，验证版本选择、同架构全选、
  不同架构禁选、勾选顺序决定首台、回滚原因、浅色与深色主题。
  浏览器未对生产或真实节点下发更新。

新增迁移已在生产备份的本地恢复副本验证后应用；先完成一台真实节点验证，
再按队列推进另外五台。安装包须保持已有环境变量与流量批次文件协议兼容。不得把
Xray/Hysteria 核心包当作 Agent 上传；此入口不更新代理核心或更新器自身。

测试入口参见 [README](README.md)。构建产物暂存项目 `.tmp/agent-updater-linux-amd64`
和 `.tmp/agent-updater-linux-arm64`，未纳入 Git。

## 生产验证

- 功能提交：`bce8521`，基于已上线的额度断连修复 `e60e30d`；
  未合入工作区未完成的 SEO 改动。发布分支 API 全套 474 项测试通过，
  15 项需显式开启数据库环境的测试跳过；新增数据库测试此前已在本地通过。
- 数据库备份 SHA-256：
  `b384f0315c8e04096b285141e308ea9e10954f70d63bb5a74841fd617d720b8b`。
  恢复副本保留 636 个用户、270 份权益；新增表和管理员权限迁移通过。
- API/Web 候选端口健康后切换，随后恢复常规端口；全程仅保留一个同步 worker。
- Agent 版本 `2026.09.15.1`，Linux amd64，24,880,066 字节；运行文件 SHA-256：
  `cc13aab0be7b496b89be032045db48347dd7b5ab7c656ac81733519300b3b81a`。
  使用已验证的 Agent 源码重新构建，仅增加发布构建标识。
- 首台 104.233.187.89 成功后，顺序更新 212.103.62.228、154.37.154.9、
  154.9.233.168、23.254.205.4、154.37.154.174，六个任务全部 SUCCEEDED。
- 六台 Xray/Hysteria 核心 PID 均未改变，授权用户数与更新前一致，
  各自流量状态文件路径保持不变，旧 Agent 二进制保留以供回滚。
- 上线后仍有 636 个用户、270 份权益、221 个订阅、256 个订单。
  最近五分钟录入 76 个流量批次，12 个启用端点同步错误均为空；
  最终健康检查无积压批次。URI 与 Clash 订阅均 HTTP 200、各含 12 个节点。
- 此次生产仅验证成功路径，未向在用节点注入故障；离线恢复、校验失败、
  健康失败回滚仍以隔离测试为证，不声称已在生产执行破坏性测试。
